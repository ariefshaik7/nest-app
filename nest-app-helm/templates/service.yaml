apiVersion: v1
kind: Service
metadata:
  name: {{ include "nest-app-helm.fullname" . }}
  labels:
    {{- include "nest-app-helm.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nest-app-helm.selectorLabels" . | nindent 4 }}
